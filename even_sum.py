## List comprehensions, while loops, for loops, if statements, and lambda functions

# Our goal is simple. We want to find the sum of all the even numbers in a list of integeres.

# NOTE: You can assume that numbers are ordered from least to greatest.

def find_even_sum(numbers):
	# Your code goes here
	pass

if __name__ == "__main__":
	assert find_even_sum([1, 2, 3, 4]) == 6
	assert find_even_sum([2, 3, 6, 7, 9]) == 8
	assert find_even_sum([-4, -1, 0, 1, 2, 3, 4, 5, 7, 8]) == 10